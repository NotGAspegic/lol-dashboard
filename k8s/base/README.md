# Base Manifests

Reusable app workloads for Farsight. These manifests assume that a `farsight`
namespace, `farsight-config` ConfigMap, `farsight-secrets` Secret, Redis, and
Postgres-compatible database are available.

Use `../local` for a complete local demo.
