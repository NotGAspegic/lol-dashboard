services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    container_name: lol_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: lol_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer:latest
    container_name: lol_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

  api:
    image: python:3.11-slim
    container_name: lol_api
    restart: unless-stopped
    working_dir: /backend
    volumes:
      - ../backend:/backend
    env_file:
      - ../backend/.env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://loluser:lolpassword@postgres:5432/loldb}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: ["sh", "-c", "echo 'API placeholder ready at /backend'; tail -f /dev/null"]

  worker:
    image: python:3.11-slim
    container_name: lol_worker
    restart: unless-stopped
    working_dir: /backend
    volumes:
      - ../backend:/backend
    env_file:
      - ../backend/.env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://loluser:lolpassword@postgres:5432/loldb}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
    depends_on: [redis, postgres]
    command:
      [
        "sh",
        "-c",
        "pip install --no-cache-dir -r requirements.txt && celery -A worker.celery_app worker -Q ingestion,refresh --loglevel=info --concurrency=2",
      ]

volumes:
  pgdata:
  redis_data:
